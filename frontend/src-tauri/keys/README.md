# Clé privée Tauri updater — NE PAS COMMITTER (voir .gitignore)
# Générée avec: pnpm exec tauri signer generate -w src-tauri/keys/expert.key --ci
#
# Build signé:
#   export TAURI_SIGNING_PRIVATE_KEY_PATH="$(pwd)/src-tauri/keys/expert.key"
#   pnpm tauri:build
#
# La clé publique est dans tauri.conf.json (plugins.updater.pubkey).
